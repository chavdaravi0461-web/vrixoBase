apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: vrixo
  labels:
    app: vrixo
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: vrixo
    component: frontend
