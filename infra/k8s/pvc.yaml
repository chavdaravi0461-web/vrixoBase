apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vrixo-uploads
  namespace: vrixo
  labels:
    app: vrixo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
